package com.github.rmielnik.demo

import android.view.View
import com.github.rmielnik.list.AbstractViewHolder

class DividerViewHolder(view: View) : AbstractViewHolder<DividerItem>(view) {

    override fun bind(item: DividerItem) {
    }
}